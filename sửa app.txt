sửa nút hamburger (3 sọc) thành nút back quay lại
Khi đọc : Nội dung hiển thị full trang, lật trang => hiển thị nội dung kế tiếp, không scroll.
Zoom chữ to nhỏ bằng hành động tay (thử xem đc không, khả năng đc)

Xóa phần header khi đọc, chỉ để lại 1 menu nhỏ cho việc điều chỉnh cỡ chữ, font chữ và màu nền. Mặc định ẩn. Hiển thị khi vuốt từ dưới lên.
Sửa code để lấy metadata từ các file import (epub) để hiển thị tên, ảnh, tác giả
Thêm nút 3 chấm dọc ở góc dưới bên phải cạnh tên sách để hiển thị menu : them vào ds yêu thích, thêm vào các collection
Bổ sung tiến độ đọc cho mỗi cuốn sách, hiển thị ở góc trên bên phải (trên bìa sách)

Phần tT và cài đặt giao diện đang trùng nhau => sửa lại để chỉ 1 menu như mục 4.

